# RhinoBoy Dashboard Configuration

# Server port
PORT=3000

# Environment (development | production)
NODE_ENV=production

# OpenClaw Gateway URL (where your OpenClaw instance runs)
GATEWAY_URL=http://localhost:18789

# Gateway authentication token (from your OpenClaw config)
GATEWAY_TOKEN=your-gateway-token-here

# Workspace path (directory for file browser)
WORKSPACE=/path/to/your/workspace
